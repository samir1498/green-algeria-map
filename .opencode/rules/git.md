# Git Rules

## NEVER push directly to main

- All changes must go through a feature branch and PR
- Create a branch, push, open PR, wait for CI
- Only merge after PR is approved or explicitly requested

## Branch naming

- Use `fix/`, `feat/`, `chore/`, `docs/` prefixes
- Keep branch names short and descriptive
